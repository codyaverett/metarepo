---
name: example-helper
description: Second example skill in the example meta module, showing that a module can ship more than one skill.
version: 0.1.0
---

# example-helper

A second skill bundled by the example module to show that `[[module.skills]]`
accepts multiple entries. Each is installed independently and audited on its own.
