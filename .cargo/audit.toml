# cargo-audit ignore list.
# Add transitive advisories we cannot patch directly; revisit each entry
# when upstream dependencies publish fixed versions.
[advisories]
ignore = [
    "RUSTSEC-2024-0436", # paste unmaintained (transitive, via ratatui)
    "RUSTSEC-2026-0002", # lru IterMut unsound (transitive)
]
